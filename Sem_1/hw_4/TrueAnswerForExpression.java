package Sem_1.hw_4;


/*
 *+Задано уравнение вида q + w = e, q, w, e >= 0.
 * Некоторые цифры могут быть заменены знаком вопроса,
 * например 2? + ?5 = 69. Требуется восстановить выражение
 * до верного равенства. Предложить хотя бы одно решение или сообщить,
 * что его нет.



public class TrueAnswerForExpression {
    static void findSolution() {
        for (int q = 0; q <= 9; q++) {
            for (int w = 0; w <= 9; w++) {
                for (int e = 0; e <= 9; e++) {
                    if (checkEquation(q, w, e)) {
                        System.out.println("Равенство восстановлено: " + q + " + " + w + " = " + e);
                        return;
                    }
                }
            }
        }
        System.out.println("Решения нет");
    }

    static boolean checkEquation(int q, int w, int e) {
        return (q + w == e);
    }
    findSolution();

}
*/